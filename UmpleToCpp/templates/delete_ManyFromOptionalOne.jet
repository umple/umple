<%@ jet package="cruise.umple.ignore" class="Ignore"%>
    for( int i =0; sizeof(<%=gen.translate("associationMany",av)%>); i++)
    {
      <%=gen.translate("associationMany",av)%>[i]-><%=gen.relatedTranslate("setMethod",av)%>(NULL);
    }
