req R1 text { System must execute background activity }

class X {
  status {
    state1 {
      implementsReq R1;
      do { keepDoing(); }
      e -> state2;
    }

    state2 {
      e -> state1;
    }
  }
}