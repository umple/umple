req R1 text { Requirement for first entry action only }

class X {
  status {
    state1 {
      implementsReq R1;
      entry / {doEntry();}
      e -> state2;
    }

    state2 {
      entry / {doAnotherEntry();}
      e -> state1;
    }
  }
}
