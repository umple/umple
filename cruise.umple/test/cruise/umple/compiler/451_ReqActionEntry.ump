req R1 text { System must process entry action }

class X {
  status {
    state1 {
      implementsReq R1;
      entry / {doSomething();}
      e -> state2;
    }

    state2 {
      e -> state1;
    }
  }
}