// Regression fixture: 3-node cycle A -> B -> C -> A, all used, root Z -> A.
// Each require statement is checked on its own, so the cycle needs no
// special handling. Expected: satisfied (true).
mixset A { require [B]; }
mixset B { require [C]; }
mixset C { require [A]; }
mixset Z { require [A]; }

use A;
use B;
use C;
