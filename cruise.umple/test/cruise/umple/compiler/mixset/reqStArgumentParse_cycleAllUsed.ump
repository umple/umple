// Regression fixture: require-cycle (A <-> B) where both nodes are used and
// a root mixset Z points into the cycle. Expected: satisfied (true).
mixset A { require [B]; }
mixset B { require [A]; }
mixset Z { require [A]; }

use A;
use B;
