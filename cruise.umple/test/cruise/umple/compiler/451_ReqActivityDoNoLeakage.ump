req R1 text { Requirement for first do activity only }

class X {
  status {
    state1 {
      implementsReq R1;
      do { keepDoing(); }
      e -> state2;
    }

    state2 {
      do { keepDoingOther(); }
      e -> state1;
    }
  }
}
