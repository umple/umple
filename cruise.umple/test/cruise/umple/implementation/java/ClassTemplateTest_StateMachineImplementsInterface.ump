interface Logger {
	boolean connect();
	boolean disconnect();
	boolean write(String Data);
}

class FileLogger{
	isA Logger;
	internal fileName;
	status{
		disconnected{
			connect -> connected;
		}
		connected{
			disconnect -> disconnected;
			write(String) -> connected;
		}	
	}
}
