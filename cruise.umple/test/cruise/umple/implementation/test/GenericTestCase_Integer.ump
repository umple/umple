
class Student {  
    
    String studentId;
    nameMemebershipId;
    nameMemebershipHouse;
    address;    
    
    
  generic test checkifLogged(String attribute.regex(name\w*)){
        Student st1 ( "S1425", "John", "Ottawa") ;        
        String valueToCheck = st1.get<<attribute>>();  
        st1.getValue(<<attribute>>);    
        boolean isLogged =  st1.checkIsLogged(valueToCheck);        
        
        //list assertions                      
        }
        
        String  returnSumOfValues (int x, int y) { return x+y;}                 
}  
 
  