
class Student
{
  const Boolean DEBUG = true;
  firstName;
  lastName;
  cityOfBirth;
  before get*,!getCityOfBirth { 
    if ( DEBUG) { System.out.println("accessing the name"); }
  }
}
//$?[End_of_model]$?
// @@@skipcompile - Issue 594 currently can't combine get* and !getCityOfBirth
