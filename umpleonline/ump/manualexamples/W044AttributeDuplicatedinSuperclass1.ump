// This example generates the warning message
class A {
  Integer attr;
}

class B {
  isA A;
  Integer attr;
}

//$?[End_of_model]$?
// @@@skipcompile - the result won't compile because there are two attr