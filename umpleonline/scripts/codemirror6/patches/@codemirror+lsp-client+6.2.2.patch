diff --git a/node_modules/@codemirror/lsp-client/dist/index.js b/node_modules/@codemirror/lsp-client/dist/index.js
index cded017..4a5eb81 100644
--- a/node_modules/@codemirror/lsp-client/dist/index.js
+++ b/node_modules/@codemirror/lsp-client/dist/index.js
@@ -62,6 +62,8 @@ function toPosition(doc, pos) {
     return { line: line.number - 1, character: pos - line.from };
 }
 function fromPosition(doc, pos) {
+    // Throws RangeError if pos is outside doc (e.g., layout tail).
+    // Call sites must catch and skip.
     let line = doc.line(pos.line + 1);
     return line.from + pos.character;
 }
@@ -1148,14 +1150,24 @@ function doRename(view, newName) {
             let lspChanges = response.changes[uri], file = plugin.client.workspace.getFile(uri);
             if (!lspChanges.length || !file)
                 continue;
-            plugin.client.workspace.updateFile(uri, {
-                changes: lspChanges.map(change => ({
-                    from: mapping.mapPosition(uri, change.range.start),
-                    to: mapping.mapPosition(uri, change.range.end),
-                    insert: change.newText
-                })),
-                userEvent: "rename"
-            });
+            let mapped = [];
+            for (let change of lspChanges) {
+                try {
+                    mapped.push({
+                        from: mapping.mapPosition(uri, change.range.start),
+                        to: mapping.mapPosition(uri, change.range.end),
+                        insert: change.newText
+                    });
+                } catch (e) { /* Skip individual change with out-of-range position */ }
+            }
+            if (mapped.length) {
+                try {
+                    plugin.client.workspace.updateFile(uri, {
+                        changes: mapped,
+                        userEvent: "rename"
+                    });
+                } catch (e) { /* Skip file if updateFile itself fails */ }
+            }
         }
     }, err => {
         plugin.reportError("Rename request failed", err);
@@ -1472,12 +1484,20 @@ function jumpToOrigin(view, type) {
         if (!response)
             return;
         let loc = Array.isArray(response) ? response[0] : response;
-        return (loc.uri == plugin.uri ? Promise.resolve(view) : plugin.client.workspace.displayFile(loc.uri)).then(target => {
+        if (!loc) return;
+        let locUri = loc.targetUri || loc.uri;
+        let locRange = loc.targetSelectionRange || loc.targetRange || loc.range;
+        if (!locUri || !locRange) return;
+        return (locUri == plugin.uri ? Promise.resolve(view) : plugin.client.workspace.displayFile(locUri)).then(target => {
             if (!target)
                 return;
-            let pos = mapping.getMapping(loc.uri) ? mapping.mapPosition(loc.uri, loc.range.start)
-                : plugin.fromPosition(loc.range.start, target.state.doc);
-            target.dispatch({ selection: { anchor: pos }, scrollIntoView: true, userEvent: "select.definition" });
+            try {
+                let pos = mapping.getMapping(locUri) ? mapping.mapPosition(locUri, locRange.start)
+                    : plugin.fromPosition(locRange.start, target.state.doc);
+                target.dispatch({ selection: { anchor: pos }, scrollIntoView: true, userEvent: "select.definition" });
+            } catch (e) {
+                // Skip — server returned a position in the hidden layout tail
+            }
         });
     }, error => plugin.reportError("Find definition failed", error)));
     return true;
@@ -1596,7 +1616,9 @@ function createReferencePanel(locs, mapping) {
         panel.role = "listbox";
         panel.setAttribute("aria-label", view.state.phrase("Reference list"));
         let options = [];
+        let visibleLocs = [];
         for (let { file, range } of locs) {
+            try {
             let fileName = file.uri.slice(prefixLen);
             if (fileName != curFile) {
                 curFile = fileName;
@@ -1622,6 +1644,8 @@ function createReferencePanel(locs, mapping) {
             if (!options.length)
                 entry.setAttribute("aria-selected", "true");
             options.push(entry);
+            visibleLocs.push({ file, range });
+            } catch (e) { /* Skip reference with out-of-range position */ }
         }
         function curSelection() {
             for (let i = 0; i < options.length; i++) {
@@ -1639,19 +1663,21 @@ function createReferencePanel(locs, mapping) {
             }
         }
         function showReference(index) {
-            let { file, range } = locs[index];
+            let { file, range } = visibleLocs[index];
             let plugin = LSPPlugin.get(view);
             if (!plugin)
                 return;
             Promise.resolve(file.uri == plugin.uri ? view : plugin.client.workspace.displayFile(file.uri)).then(view => {
                 if (!view)
                     return;
+                try {
                 let pos = mapping.mapPosition(file.uri, range.start, 1);
                 view.focus();
                 view.dispatch({
                     selection: { anchor: pos },
                     scrollIntoView: true
                 });
+                } catch (e) { /* Skip — position in layout tail */ }
             });
         }
         panel.addEventListener("keydown", event => {
@@ -1660,10 +1686,10 @@ function createReferencePanel(locs, mapping) {
                 view.focus();
             }
             else if (event.keyCode == 38 || event.keyCode == 33) { // ArrowUp, PageUp
-                setSelection((curSelection() - 1 + locs.length) % locs.length);
+                setSelection((curSelection() - 1 + visibleLocs.length) % visibleLocs.length);
             }
             else if (event.keyCode == 40 || event.keyCode == 34) { // ArrowDown, PageDown
-                setSelection((curSelection() + 1) % locs.length);
+                setSelection((curSelection() + 1) % visibleLocs.length);
             }
             else if (event.keyCode == 36) { // Home
                 setSelection(0);
@@ -1760,13 +1786,17 @@ function serverDiagnostics() {
                     return false;
                 view.dispatch(setDiagnostics(view.state, params.diagnostics.map(item => {
                     var _a;
-                    return ({
-                        from: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.start, plugin.syncedDoc)),
-                        to: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.end, plugin.syncedDoc)),
-                        severity: toSeverity((_a = item.severity) !== null && _a !== void 0 ? _a : 1),
-                        message: item.message,
-                    });
-                })));
+                    try {
+                        return ({
+                            from: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.start, plugin.syncedDoc)),
+                            to: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.end, plugin.syncedDoc)),
+                            severity: toSeverity((_a = item.severity) !== null && _a !== void 0 ? _a : 1),
+                            message: item.message,
+                        });
+                    } catch (e) {
+                        return null; // Skip diagnostics with out-of-range positions (layout tail)
+                    }
+                }).filter(d => d !== null)));
                 return true;
             }
         },
diff --git a/node_modules/@codemirror/lsp-client/src/definition.ts b/node_modules/@codemirror/lsp-client/src/definition.ts
index 1f4341f..a711faa 100644
--- a/node_modules/@codemirror/lsp-client/src/definition.ts
+++ b/node_modules/@codemirror/lsp-client/src/definition.ts
@@ -37,11 +37,19 @@ function jumpToOrigin(view: EditorView, type: {get: typeof getDefinition, capabi
   plugin.client.withMapping(mapping => type.get(plugin, view.state.selection.main.head).then(response => {
     if (!response) return
     let loc = Array.isArray(response) ? response[0] : response
-    return (loc.uri == plugin.uri ? Promise.resolve(view) : plugin.client.workspace.displayFile(loc.uri)).then(target => {
+    if (!loc) return // Guard empty Location[]
+    let locUri = (loc as any).targetUri ?? loc.uri
+    let locRange = (loc as any).targetSelectionRange ?? (loc as any).targetRange ?? loc.range
+    if (!locUri || !locRange) return
+    return (locUri == plugin.uri ? Promise.resolve(view) : plugin.client.workspace.displayFile(locUri)).then(target => {
       if (!target) return
-      let pos = mapping.getMapping(loc.uri) ? mapping.mapPosition(loc.uri, loc.range.start)
-        : plugin.fromPosition(loc.range.start, target.state.doc)
-      target.dispatch({selection: {anchor: pos}, scrollIntoView: true, userEvent: "select.definition"})
+      try {
+        let pos = mapping.getMapping(locUri) ? mapping.mapPosition(locUri, locRange.start)
+          : plugin.fromPosition(locRange.start, target.state.doc)
+        target.dispatch({selection: {anchor: pos}, scrollIntoView: true, userEvent: "select.definition"})
+      } catch (e) {
+        // Skip — server returned a position in the hidden layout tail
+      }
     })
   }, error => plugin.reportError("Find definition failed", error)))
   return true
diff --git a/node_modules/@codemirror/lsp-client/src/diagnostics.ts b/node_modules/@codemirror/lsp-client/src/diagnostics.ts
index aa4167f..3ec00a6 100644
--- a/node_modules/@codemirror/lsp-client/src/diagnostics.ts
+++ b/node_modules/@codemirror/lsp-client/src/diagnostics.ts
@@ -34,12 +34,18 @@ export function serverDiagnostics(): LSPClientExtension {
         if (!file || params.version != null && params.version != file.version) return false
         const view = file.getView(), plugin = view && LSPPlugin.get(view)
         if (!view || !plugin) return false
-        view.dispatch(setDiagnostics(view.state, params.diagnostics.map(item => ({
-          from: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.start, plugin.syncedDoc)),
-          to: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.end, plugin.syncedDoc)),
-          severity: toSeverity(item.severity ?? 1),
-          message: item.message,
-        }))))
+        view.dispatch(setDiagnostics(view.state, params.diagnostics.map(item => {
+          try {
+            return {
+              from: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.start, plugin.syncedDoc)),
+              to: plugin.unsyncedChanges.mapPos(plugin.fromPosition(item.range.end, plugin.syncedDoc)),
+              severity: toSeverity(item.severity ?? 1),
+              message: item.message,
+            }
+          } catch (e) {
+            return null // Skip diagnostics with out-of-range positions (layout tail)
+          }
+        }).filter(d => d !== null) as any))
         return true
       }
     },
diff --git a/node_modules/@codemirror/lsp-client/src/references.ts b/node_modules/@codemirror/lsp-client/src/references.ts
index db57a44..c82dba4 100644
--- a/node_modules/@codemirror/lsp-client/src/references.ts
+++ b/node_modules/@codemirror/lsp-client/src/references.ts
@@ -82,7 +82,9 @@ function createReferencePanel(locs: readonly ReferenceLocation[], mapping: Works
     panel.role = "listbox"
     panel.setAttribute("aria-label", view.state.phrase("Reference list"))
     let options: HTMLElement[] = []
+    let visibleLocs: typeof locs = []
     for (let {file, range} of locs) {
+      try {
       let fileName = file.uri.slice(prefixLen)
       if (fileName != curFile) {
         curFile = fileName
@@ -105,6 +107,8 @@ function createReferencePanel(locs: readonly ReferenceLocation[], mapping: Works
       if (textAfter) entry.appendChild(document.createTextNode(textAfter))
       if (!options.length) entry.setAttribute("aria-selected", "true")
       options.push(entry)
+      visibleLocs.push({file, range})
+      } catch (e) { /* Skip reference with out-of-range position */ }
     }
 
     function curSelection() {
@@ -120,17 +124,19 @@ function createReferencePanel(locs: readonly ReferenceLocation[], mapping: Works
       }
     }
     function showReference(index: number) {
-      let {file, range} = locs[index]
+      let {file, range} = visibleLocs[index]
       let plugin = LSPPlugin.get(view)
       if (!plugin) return
       Promise.resolve(file.uri == plugin.uri ? view : plugin.client.workspace.displayFile(file.uri)).then(view => {
         if (!view) return
+        try {
         let pos = mapping.mapPosition(file.uri, range.start, 1)
         view.focus()
         view.dispatch({
           selection: {anchor: pos},
           scrollIntoView: true
         })
+        } catch (e) { /* Skip — position in layout tail */ }
       })
     }
 
@@ -139,9 +145,9 @@ function createReferencePanel(locs: readonly ReferenceLocation[], mapping: Works
         closeReferencePanel(view)
         view.focus()
       } else if (event.keyCode == 38 || event.keyCode == 33) { // ArrowUp, PageUp
-        setSelection((curSelection() - 1 + locs.length) % locs.length)
+        setSelection((curSelection() - 1 + visibleLocs.length) % visibleLocs.length)
       } else if (event.keyCode == 40 || event.keyCode == 34) { // ArrowDown, PageDown
-        setSelection((curSelection() + 1) % locs.length)
+        setSelection((curSelection() + 1) % visibleLocs.length)
       } else if (event.keyCode == 36) { // Home
         setSelection(0)
       } else if (event.keyCode == 35) { // End
diff --git a/node_modules/@codemirror/lsp-client/src/rename.ts b/node_modules/@codemirror/lsp-client/src/rename.ts
index 0a639d8..0f37470 100644
--- a/node_modules/@codemirror/lsp-client/src/rename.ts
+++ b/node_modules/@codemirror/lsp-client/src/rename.ts
@@ -55,14 +55,24 @@ function doRename(view: EditorView, newName: string) {
     uris: for (let uri in response.changes) {
       let lspChanges = response.changes[uri], file = plugin.client.workspace.getFile(uri)
       if (!lspChanges.length || !file) continue
-      plugin.client.workspace.updateFile(uri, {
-        changes: lspChanges.map(change => ({
-          from: mapping.mapPosition(uri, change.range.start),
-          to: mapping.mapPosition(uri, change.range.end),
-          insert: change.newText
-        })),
-        userEvent: "rename"
-      })
+      let mapped: {from: number, to: number, insert: string}[] = []
+      for (let change of lspChanges) {
+        try {
+          mapped.push({
+            from: mapping.mapPosition(uri, change.range.start),
+            to: mapping.mapPosition(uri, change.range.end),
+            insert: change.newText
+          })
+        } catch (e) { /* Skip individual change with out-of-range position */ }
+      }
+      if (mapped.length) {
+        try {
+          plugin.client.workspace.updateFile(uri, {
+            changes: mapped,
+            userEvent: "rename"
+          })
+        } catch (e) { /* Skip file if updateFile itself fails */ }
+      }
     }
   }, err => {
     plugin.reportError("Rename request failed", err)
