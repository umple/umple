[umple]
user=php
pm=ondemand
pm.max_children=8
listen=/var/run/php-fpm.sock
listen.owner=nginx
catch_workers_output=true
; Pass LSP config to PHP so umple.php can generate auth tokens
env[UMPLE_LSP_WS_URL] = $UMPLE_LSP_WS_URL
env[LSP_AUTH_SECRET] = $LSP_AUTH_SECRET
