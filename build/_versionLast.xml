<?xml version="1.0" encoding="UTF-8"?>
<project basedir=".." name="VersionLast" >

  <property prefix="last" file="build/umpleversion.last.txt"/>
  <property name="umple.version" value="${last.version}" />
  <echo message="Last Version: ${last.version}" />

</project>

