W071 Duplicate Method Different Type
Errors and Warnings
noreferences

@@description

<h2>Umple semantic warning reported when two methods have the same class names but return different types</h2>

<p>
In some programming languages like Java, you cannot have the same methods' names even the return types are different. The warning is shown to notify the developer of the potential mistake.

</p>


@@example
@@source manualexamples/W071DupMethodNameDiffType1.ump
@@endexample

@@example
@@source manualexamples/W071DupMethodNameDiffType2.ump
@@endexample
